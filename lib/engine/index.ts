/**
 * lib/engine/index.ts — public barrel for the CliffCheck benefit engine.
 *
 * Placeholder. task-108 extracts the pure benefit-calculation functions and
 * gov-cited rule tables from the v1 single-file build (_archive/index.html) into
 * typed modules here (snap.ts, aca.ts, medicaid.ts, …, states/*.ts) and wires
 * the public API: getEffectiveTakeHome, getCliffData, getSupportedStates,
 * isSupportedState, getStateSources, getHeroScenarios.
 * See docs/replatform/PLAN.md §"Engine extraction".
 */
export {};
